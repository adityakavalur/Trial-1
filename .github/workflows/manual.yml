name: env check

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Environment check
      run: |
        whoami
        hostname
        
